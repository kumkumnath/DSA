class Solution(object):
    def isPalindrome(self, x):
      reverse = 0
      orignal = x
      while x > 0:
           digit = x % 10
           reverse = reverse * 10 + digit
           x = x // 10
      if reverse == orignal:
         return True
      else:
          return False   
