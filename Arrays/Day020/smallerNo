class Solution(object):
    def smallerNumbersThanCurrent(self, nums):
      ans=[]
      for num in nums:
          count=0
          for other in nums:
              if other < num:
                 count+=1
          ans.append(count)
      return ans 
